{
  "name": "elevator-simulator-server",
  "version": "1.0.0",
  "private": true,
  "description": "OOP elevator simulator backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
